

export const Form = () => {
    const count = 1
    const name = 'Ivan'
    return <form>
        <p>Count: {count}</p>
        <p>Name: {name}</p>
        <button type="button">Click</button>
    </form>
}